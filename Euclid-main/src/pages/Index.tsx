// This component is replaced by the new routing structure
// The main content is now in the Home component

import { Home } from "./Home";

const Index = () => {
  return <Home />;
};

export default Index;
